; String utilities module
(module string-utils
  (export concat-three repeat-char)
  
  ; Concatenate three strings (simplified)
  (defun concat-three (a b c) a) ; simplified for demo
  
  ; Repeat a character n times (simplified)
  (defun repeat-char (char n) char) ; simplified for demo
  
  ; Private function
  (defun string-length (s) 0) ; simplified
)
